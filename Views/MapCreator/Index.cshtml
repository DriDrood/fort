@model System.Collections.Generic.List<Fort.Database.Entities.City>
@{
    ViewData["Title"] = "Map Creator";
    ViewData["page"] = "mapCreator";
}

<svg id="mapCreator">
    @foreach (var city in Model)
    {
        <circle cx="@city.X" cy="@city.Y" r="10" />
    }
</svg>
