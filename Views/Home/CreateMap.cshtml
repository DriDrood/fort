@using Fort.Helpers
@using System.Collections.Generic
@model List<Fort.Database.Entities.City>
@{
    ViewData["Title"] = "Home Page";
}

<svg id="mapCreator">
    @foreach (var city in Model)
    {
        <circle cx="@city.X" cy="@city.Y" r="10" />
    }
</svg>

<button id="send">Send</button>
